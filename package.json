{
  "name": "shelter-network-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.js --port=4173",
    "build": "node scripts/build.js",
    "preview": "node scripts/serve.js --dist --port=4173",
    "lint": "node --check src/main.js && node --check src/data/runHistory.js && node --check scripts/build.js && node --check scripts/serve.js",
    "test": "npm run lint"
  }
}
